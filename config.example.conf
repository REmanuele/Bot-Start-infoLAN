sudo mkdir -p /etc/telegram-boot-bot
sudo tee /etc/telegram-boot-bot/config.conf >/dev/null <<'EOF'
BOT_TOKEN="INSERISCI_IL_TUO_BOT_TOKEN"
CHAT_ID="INSERISCI_IL_TUO_CHAT_ID"
# opzionale: testo extra in coda al messaggio di boot
EXTRA_MSG=""
EOF
sudo chmod 600 /etc/telegram-boot-bot/config.conf
